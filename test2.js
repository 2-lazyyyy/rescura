const fs = require("fs"); const path = require("path"); function walk(d) { const files = fs.readdirSync(d); for(let f of files) { let fp = path.join(d,f); if(fs.statSync(fp).isDirectory()) { walk(fp); } else if(fp.endsWith(".ts")) { const c = fs.readFileSync(fp,"utf8"); let ms = c.match(/\.from\([\x27\x22]([^\x27\x22]+)[\x27\x22]\)[^;]+?(?=\n|$)/g); if(ms) ms.forEach(m => console.log(m.replace(/\s+/g," "))); } } } walk("./src/services");
